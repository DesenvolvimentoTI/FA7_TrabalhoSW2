<phone:PhoneApplicationPage
    x:Class="ForcaVenda.views.Consulta"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <StackPanel Orientation="Vertical">
        <TextBlock 
            Text="CONSULTAS" 
            TextAlignment="Center"
            Padding="20"
            Margin="60"
            FontSize="42"/>

        <Button Name="btHistorico" Content="HISTÓRICO" Padding="20" Margin="10"/>
        <Button Name="btEstoque" Content="ESTOQUE" Click="btEstoque_Click" Padding="20" Margin="10"/>
        <Button Name="btPedido" Content="PEDIDO DE VENDA" Padding="20" Margin="10"/>
    </StackPanel>


</phone:PhoneApplicationPage>